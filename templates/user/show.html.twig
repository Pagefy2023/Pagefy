{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1 class="title">Utilisateur</h1>

    <a class="btnb" href="{{ path('app_user_index') }}">Revenir à la liste</a>
    <a class="btnb" href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a>
    
    <table class="table">
        <tbody>

            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>

        </tbody>
    </table>




{% endblock %}
