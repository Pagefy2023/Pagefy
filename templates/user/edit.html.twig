{% extends 'base.html.twig' %}

{% block title %} Modification de Roles {% endblock %}

{% block body %}
    <h1 class="title">Modification de Roles</h1>

    <a class="btnb" href="{{ path('app_user_index') }}">Revenir à la liste</a>

<div class="div-form-style">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="position">
        <button class="btnb">{{ button_label|default('Modifier') }}</button>
    </div>
    {{ form_end(form) }}
</div>


    {{ include('user/_delete_form.html.twig') }}
{% endblock %}



